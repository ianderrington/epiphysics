import { ComponentType } from 'react';
import DefaultHomePage, { HomePageProps } from '@/layouts/default/home';

// Custom components
const CustomHero: ComponentType = () => (
  <div className="text-center">
    <h1 className="text-6xl font-bold mb-4">Welcome to My Site</h1>
    <p className="text-xl">A custom hero section with unique styling</p>
  </div>
);

const CustomFeatures: ComponentType = () => (
  <div className="text-center">
    <h2 className="text-4xl font-bold mb-8">Custom Features Section</h2>
    {/* Add your custom features content */}
  </div>
);

const CustomCTA: ComponentType = () => (
  <div>
    <h2 className="text-3xl font-bold text-white mb-4">Get Started Today</h2>
    <button className="bg-white text-blue-600 px-8 py-3 rounded-lg">
      Contact Me
    </button>
  </div>
);

// Export the custom home page with overridden components
export default function CustomHomePage(props: HomePageProps) {
  return (
    <DefaultHomePage
      {...props}
      hero={CustomHero}
      features={CustomFeatures}
      cta={CustomCTA}
      className="custom-home-page"
    />
  );
} 
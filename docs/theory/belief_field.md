---
title: "Belief Fields and Potential Fields — What Entities Think Will Happen vs What Could"
description: >-
  Every computational entity carries a probabilistic model of future states — a belief field.
  Reality has its own potential field. The gap between them is where learning, delusion,
  preparation, and surprise live.
date: 2026-03-18T00:00:00.000Z
draft: true
author:
  name: "Ian Derrington"
contentType: article
series: "Epimechanics"
categories:
  - Physics
  - Systems thinking
tags:
  - Belief
  - Prediction
  - Potential fields
  - Intelligence
  - Representations
coverImage:
  url: ./images/belief_field-1-1.png
  alt: >-
    Two overlapping translucent landscapes — one sharp and real (the actual potential field),
    one blurred and colored by the entity's perspective (the belief field). Where they align,
    the entity navigates well. Where they diverge, surprise or delusion.
tts:
  enabled: true
  provider: openai
  voice: onyx
  enableSpeed: true
feedback:
  enabled: true
---

## Two Landscapes

Every entity that can predict — every entity with nonzero intelligence $I$ ([Part 3](./03_intelligence_consciousness_agency.md)) — carries two things:

1. **The actual potential field** $V(X)$ — the real landscape of forces, attractors, barriers, and gradients that the entity moves through. This exists regardless of whether the entity knows about it. It is the territory.

2. **The belief field** $\hat{V}(X, t)$ — the entity's probabilistic model of that landscape. What the entity *thinks* the potential field looks like, based on its past experience, its current observations, and its capacity to predict. This is the map.

The entity acts on $\hat{V}$, not on $V$. It navigates by its belief field, not by reality — because it cannot access reality directly ([Hoffman's Interface Theory](https://doi.org/10.1093/oso/9780393254693.001.0001); [Part 0](./00_prelude.md)). Every decision, every action, every preparation is based on $\hat{V}$. The quality of those decisions depends on how well $\hat{V}$ tracks $V$.

---

## What a Belief Field Is

A belief field is a probability distribution over future states, conditioned on the entity's current knowledge:

$$\hat{V}(X', t+\Delta t \mid X, t) = P(X' \mid X, M_E)$$

where $M_E$ is the entity's internal model ([Part 3](./03_intelligence_consciousness_agency.md)) and $X'$ ranges over all states the entity considers possible. This is not a single prediction — it is a *field* of weighted possibilities. The entity assigns higher probability to some futures and lower to others, creating a landscape of anticipated potential.

The belief field has structure:

- **Peaks** — states the entity considers likely. "I expect this to happen."
- **Valleys** — states the entity considers unlikely. "I don't think this will happen."
- **Width** — how uncertain the entity is. A sharp peak means high confidence; a broad distribution means uncertainty.
- **Horizon** — how far into the future the field extends. Some entities predict seconds ahead (a thermostat). Others predict decades (a strategic institution). The horizon is bounded by intelligence $I$ and the predictability of the domain.

> [!sidenote]
> *Connection to POMDP.* The belief field is the spatial generalization of the belief state $b(s)$ from [decision theory](../applications/decision_and_trajectory.md). Where $b(s)$ asks "what state am I in?", the belief field $\hat{V}$ asks "what does the entire landscape of possible futures look like from here?"

---

## What the Actual Potential Field Is

The actual potential field $V(X)$ is what Epimechanics defines in [Part 1](./01_generalized_mechanics.md): the landscape of forces that act on any entity at position $X$ in state space. It is generated by all other entities via the mean-field construction:

$$V(X) = \int \phi(X, X') \rho(X') dX'$$

$V(X)$ exists whether or not any entity models it. It is not a belief — it is the aggregate of all actual causal forces in the system. A market has a potential landscape whether or not any trader understands it. A social norm field exists whether or not any individual can articulate it.

But $V(X)$ is also not fixed. It changes as entities move, as populations shift, as institutions restructure. The landscape the entity is trying to predict is itself evolving — which is what makes prediction hard and belief fields imperfect.

---

## The Gap: Where Everything Interesting Happens

The deviation $\Delta V = \hat{V} - V$ — the gap between what the entity believes and what is actually the case — is where all interesting dynamics live.

| $\Delta V$ | What it means | Examples |
|---|---|---|
| $\Delta V \approx 0$ | Belief tracks reality. Entity navigates well. | Expert in a stable domain; well-calibrated instrument |
| $\hat{V}$ has peaks where $V$ has valleys | Entity expects something that won't happen. **Delusion.** | Bubble investor; overconfident strategist |
| $\hat{V}$ has valleys where $V$ has peaks | Entity doesn't expect something that will happen. **Surprise.** | Black swan events; paradigm shifts |
| $\hat{V}$ is narrow, $V$ is broad | Entity is overconfident. **Fragile.** | Specialist in a volatile environment |
| $\hat{V}$ is broad, $V$ is narrow | Entity is underconfident. **Wasteful.** | Excessive hedging; analysis paralysis |
| $\hat{V}$ lags $V$ | Entity's model is outdated. **Inertia.** | Institutional beliefs that no longer match the environment |

> [!caveat]
> Neither $\hat{V}$ nor $V$ is directly observable. We infer $\hat{V}$ from the entity's behavior (what it prepares for, what it ignores) and $V$ from outcomes (what actually happens). Both inferences are uncertain.

---

## Instinct vs Cognition: Two Sources of $\hat{V}$

The belief field has (at least) two sources, operating at different timescales:

### Instinctive belief field

Evolved over generations. Encoded in neural architecture, reflexes, innate biases. The instinctive $\hat{V}$ represents the potential landscapes that the entity's ancestors regularly encountered. It is fast, automatic, and domain-specific.

A gazelle's instinctive $\hat{V}$ has a strong peak at "predator may appear from tall grass" — not because the gazelle has computed this, but because ancestors without this peak were eaten. The instinctive belief field is a compressed, inherited model of ancestral $V$'s.

In Epimechanics' terms: instinctive $\hat{V}$ is a low-resolution, high-speed approximation of $V$ trained on evolutionary timescales. Its strength is speed (no computation required). Its weakness is that it only tracks $V$ for environments similar to the ancestral environment — when $V$ changes (novel environments, technological disruption), instinctive $\hat{V}$ diverges.

### Cognitive belief field

Constructed by the entity's intelligence $I$ in real time. Updated by observation, reasoning, and learning. The cognitive $\hat{V}$ represents the entity's best current model of the potential landscape.

A strategic analyst's cognitive $\hat{V}$ incorporates data, models, scenarios, and uncertainty estimates. It is slower than instinct but more flexible — it can track $V$ even in novel environments, provided the entity has enough intelligence and data.

In Epimechanics' terms: cognitive $\hat{V}$ is a higher-resolution, lower-speed model that can be updated in real time. Its strength is adaptability. Its weakness is computational cost — maintaining an accurate $\hat{V}$ requires energy (information processing has thermodynamic cost; [Landauer's principle](https://doi.org/10.1147/rd.53.0183)).

### The interaction

Most entities operate on a blend: instinct provides the fast prior, cognition updates it with evidence. The Bayesian update from [Part 6](../applications/decision_and_trajectory.md) formalizes this:

$$\hat{V}_{\text{posterior}} = \hat{V}_{\text{instinct}} + \text{evidence update from cognitive processing}$$

When instinct and cognition agree ($\hat{V}_{\text{instinct}} \approx \hat{V}_{\text{cognitive}}$), the entity acts decisively. When they conflict, the entity experiences anxiety, hesitation, or internal tension — a measurable force in the psychological state space.

---

## Preparation as Belief-Field Optimization

An entity that prepares for the future is optimizing its state to perform well across the range of futures its belief field assigns nonzero probability. Preparation is $\hat{V}$-driven action:

- **Insurance** = preparing for $\hat{V}$ peaks that have bad outcomes (hedging against anticipated risks)
- **Investment** = preparing for $\hat{V}$ peaks that have good outcomes (positioning for anticipated opportunities)
- **Training** = broadening the entity's capacity to respond to a wider range of $\hat{V}$ outcomes
- **Denial** = narrowing $\hat{V}$ to exclude states the entity doesn't want to consider (reducing the belief field to avoid anxiety, at the cost of increased $\Delta V$)

The entity's **free energy** $\mathcal{F} = E - \mathcal{T}S$ determines how much preparation it can afford. Preparation costs energy. An entity with low $\mathcal{F}$ cannot prepare broadly — it must bet on a narrow slice of $\hat{V}$, making it fragile to surprises.

> [!tip]
> Friston's Free Energy Principle says organisms minimize variational free energy — the divergence between their internal model and sensory data. This IS belief-field optimization: minimizing $\Delta V$ by updating $\hat{V}$ to track $V$ more closely.

---

## Collective Belief Fields

When many entities share representations — through language, culture, institutions, media — their individual $\hat{V}$'s overlap and reinforce each other, creating a **collective belief field** $\hat{V}_{\text{collective}}$.

$$\hat{V}_{\text{collective}}(X) = \sum_j w_j \hat{V}_j(X)$$

This is the social analogue of the mean-field construction. The collective belief field is a reputation field ([applications plan](../applications-plan.md)) applied to the future: the aggregate of what everyone thinks will happen.

Collective belief fields have properties that individual ones don't:

- **Self-fulfilling**: if enough entities believe a bank will fail, they withdraw deposits, and the bank fails. $\hat{V}_{\text{collective}}$ modifies $V$ to match itself. The belief creates the reality it predicted.
- **Self-defeating**: if enough entities believe a stock will rise, they buy, the price rises, then reality fails to support the price, and it crashes. $\hat{V}_{\text{collective}}$ temporarily modifies $V$ then diverges catastrophically.
- **Manipulable**: propaganda distorts $\hat{V}_{\text{collective}}$ by injecting false representations ([representational manipulation](../research/representational_manipulation.md)). The collective navigates by a deliberately corrupted map.

---

## The Representational Efficiency Connection

The [Representational Efficiency principle](./05_ontology_and_open_questions.md) says the optimal representation $X^*$ minimizes predictive cost. Applied to belief fields: the optimal $\hat{V}$ is the one that predicts $V$ at minimal computational cost.

This means: entities under computational constraints (which is all entities) cannot maintain a perfect $\hat{V}$. They must compress. The structure of that compression — which futures they track and which they ignore — determines their vulnerability profile. An entity that compresses by ignoring rare events is efficient in normal times and catastrophically wrong in crises. An entity that tracks rare events pays a constant computational overhead for robustness it rarely needs.

The belief field IS the entity's compressed representation of the potential field. Its structure reveals what the entity has decided to predict and what it has decided to ignore. Both decisions have consequences.

---

## Connection to the Series

| Concept | Where developed | Belief field connection |
|---|---|---|
| Intelligence $I$ | [Part 3](./03_intelligence_consciousness_agency.md) | Determines the resolution and accuracy of $\hat{V}$ |
| Consciousness $C$ | [Part 3](./03_intelligence_consciousness_agency.md) | Determines the scope of $\hat{V}$ — how much of state space the entity models |
| Belief state $b$ | [Decision theory](../applications/decision_and_trajectory.md) | The point-estimate version of $\hat{V}$ at the current state |
| Potential $V(X)$ | [Part 1](./01_generalized_mechanics.md) | The actual landscape $\hat{V}$ is trying to approximate |
| Representational Efficiency | [Part 5](./05_ontology_and_open_questions.md) | Optimal $\hat{V}$ minimizes predictive cost |
| Representational manipulation | [Research note](../research/representational_manipulation.md) | Deliberately distorting $\hat{V}_{\text{collective}}$ |
| Free Energy Principle | [Part 1, Section 4b](./01_generalized_mechanics.md) | Organisms minimize $\Delta V$ by updating $\hat{V}$ toward $V$ |
